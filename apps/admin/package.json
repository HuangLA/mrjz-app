{
  "name": "@mrjz/admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "tsc -p tsconfig.json && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"TODO: add admin lint\"",
    "test": "echo \"TODO: add admin tests\""
  },
  "dependencies": {
    "@mrjz/shared": "0.1.0"
  }
}
