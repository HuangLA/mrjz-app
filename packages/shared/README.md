# Shared Package

Shared TypeScript types and constants used by API, Web Admin, mobile web, and mini program.

Rules:

- Keep browser-safe exports in this package.
- Do not import Node-only modules here.
- Prefer domain types from docs before adding new fields.
